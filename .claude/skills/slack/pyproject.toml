[project]
name = "nanoclaw-slack"
version = "0.1.0"
description = "Slack integration for NanoClaw using uv runtime"
requires-python = ">=3.11"
dependencies = [
    "slack-sdk>=3.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
